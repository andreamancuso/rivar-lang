class ACCOUNT

feature
    balance: INTEGER

    deposit(amount: INTEGER)
        require amount > 0
        do
            this->balance = this->balance + amount
        ensure
            balance == old balance + amount
        end

end
